import { Fragment } from 'react';
import Items from './SearchPageModel';

const SearchFunction = () => {
      return (
          <Fragment>
            <Items />
          </Fragment>
      );
};

export default SearchFunction;
